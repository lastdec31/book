import "./globals.css"; //전역 CSS

export default function RootLayout({ children }) {
  return (
    <html lang="ko">
      <body>
        <header>asdada</header>
        {children}
      </body>
    </html>
  );
}
